#include "car_buyer.hpp"
using namespace std;

/* Use this to implement CarBuyer for yourself. */

double CarBuyer::lowestCost(const vector <string> &cars, 
                            int fuel_price, 
                            int annual_distance, 
                            int years)
{
  (void) cars;                  // These lines silence compiler warnings.
  (void) fuel_price;
  (void) annual_distance;
  (void) years;

  return 0;
}
